import { FilterIContentPipe } from './filter-icontent.pipe';

describe('FilterIContentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIContentPipe();
    expect(pipe).toBeTruthy();
  });
});
